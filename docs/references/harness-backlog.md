# 하네스 백로그 — 보류된 업그레이드 (트리거로 재검토)

지금은 값싸게(프로즈로) 두고, **무엇이 관찰되면 승격할지**를 트리거로 적어둔다.
잊지 않기 위한 장치: retro가 이 목록의 트리거 충족 여부를 훑고(②), briefing이 미결 건수를 띄운다(③).

- 미결 항목은 `- [ ]`, 승격·반영되면 `- [x]` 로 바꾸고 아래 "졸업"으로 옮긴다.
- 형식: 결정(지금은 무엇을) · 보류 이유 · **승격 트리거**(무엇이 관찰되면) · 근거.

## 미결 (보류 중)

(없음)

## 졸업 (반영됨 → LESSONS.md로)

- [x] **모델링 체크리스트 → 게이트 강제 승격** (2026-07-23 → 승격 2026-07-31)
  - 반영: `gates/run-gates.mjs` 에 `model/BEFORE_ENTITIES` — 0009+ 마이그레이션은 `-- spec:` 헤더로 approved 스펙 귀속을 신고, 게이트가 존재+승인을 검증. 기득권은 스펙 이전 8개 파일명 배열(LEGACY)로 면제 — 낮은 번호 재사용 신규 파일은 헤더 요구.
  - 트리거 충족: PRODUCT 다이어트에서 모델링 플래그가 문서 밖으로 샜고(→ `_cross-cutting.md`), 스펙 없이 마이그레이션부터 작성하는 우회를 기계로 막기로 결정(주제12 뒷문 단속).
